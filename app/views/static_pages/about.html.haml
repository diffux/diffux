- provide :title, 'About'

%h1 About
%p Diffux is a tool that takes snapshots of web pages and creates a diff from a baseline snapshot.
%p
  Github:
  - link = 'https://github.com/trotzig/diffux'
  = link_to link, link
