%p#notice= notice

%p
  %b Address:
  = @url.address

%h2 Snapshots
= render @url.snapshots

= form_for Snapshot.new do |f|
  = hidden_field_tag :url, @url.id
  = f.submit 'New snapshot', class: 'btn btn-primary'

= link_to 'Edit', edit_url_path(@url)
\|
= link_to 'Back', urls_path
